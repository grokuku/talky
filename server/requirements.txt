# Serveur talky (faster-whisper) — dépendances
# torch est installé séparément dans le Dockerfile (wheel CUDA cu128).
fastapi>=0.115
uvicorn[standard]>=0.30
websockets>=12.0
python-multipart>=0.0.9
numpy>=1.26
faster-whisper==1.1.1
requests>=2.28
huggingface_hub>=0.24
silero-vad==6.2.1
